apiVersion: v1
kind: Service
metadata:
  name: {{ include "presto-app.fullname" . }}
  labels:
    {{- include "presto-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "presto-app.selectorLabels" . | nindent 4 }}
