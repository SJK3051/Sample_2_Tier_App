apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "presto-app.fullname" . }}-test-connection"
  labels:
    {{- include "presto-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "presto-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
